{
  "name": "middleware",
  "version": "1.0.0",
  "description": "create a middleware that checks is user is authenticated or not",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Niranjan Rai",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
